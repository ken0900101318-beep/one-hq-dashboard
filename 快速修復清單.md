# 總部戰情室 - 緊急修復清單

**問題回報時間**: 2026-02-19 14:52  
**問題數量**: 3個

---

## 問題 1：新增商家失敗

### 可能原因
1. ❌ 後端 `doPost` 函數未部署
2. ❌ Apps Script 未重新部署（還在用舊版）
3. ❌ CORS 問題

### 立即檢查步驟

#### 步驟 1：確認後端代碼（30秒）
打開你的 Apps Script，確認是否有這兩個函數：

```javascript
function doPost(e) {
  // 必須有這個函數
}

function addMerchant(data) {
  // 必須有這個函數
}
```

**如果沒有**：
1. 複製下方完整代碼
2. 貼到 Apps Script 最後面
3. 儲存

#### 步驟 2：重新部署（1分鐘）⚠️ 關鍵
1. 點選「部署」→「管理部署作業」
2. 點選「編輯」（鉛筆圖示）
3. **版本**：選「新版本」（不是編輯現有版本）
4. 說明：「v1.2 - 新增 doPost」
5. 點選「部署」

#### 步驟 3：測試（30秒）
1. 複製新的 Web App URL（應該一樣，但確認一下）
2. 回到戰情室
3. 重新連接（或重新整理頁面）
4. 再次測試新增商家

---

## 問題 2：店家監控顯示「開發中」

### 現況
目前只顯示店家數量，沒有詳細資訊。

### 快速修復
我會更新前端，改成顯示：
- ✅ 商家分組
- ✅ 每個商家的店家列表
- ✅ 店家基本資訊

---

## 問題 3：每次需要重新設定網址、沒有密碼保護

### 問題分析
1. localStorage 應該有儲存，但可能被清除
2. 密碼保護功能確實沒做

### 解決方案

#### A. 網址儲存問題
檢查瀏覽器是否：
- 隱私模式（無法儲存 localStorage）
- 清除 Cookie/快取時一併清除

#### B. 密碼保護
我會加入：
- ✅ 初次進入要求密碼
- ✅ 密碼：`zxc88888`
- ✅ 密碼儲存在 localStorage
- ✅ 密碼錯誤 3 次鎖定

---

## 🚀 快速修復計畫（10分鐘）

### 我現在做（5分鐘）
1. ✅ 加入密碼保護頁面
2. ✅ 改善店家監控顯示
3. ✅ 加強 localStorage 儲存
4. ✅ 推送到 GitHub

### 你需要做（5分鐘）
1. ⏳ 等待 GitHub Pages 更新（1-2分鐘）
2. ✅ 重新整理頁面（Ctrl+Shift+R）
3. ✅ 確認後端已重新部署
4. ✅ 測試功能

---

## 📋 完整後端代碼（複製貼上）

如果你的 Apps Script 沒有 `doPost` 和 `addMerchant`，完整複製這段加到最後：

```javascript
/**
 * POST 請求處理
 */
function doPost(e) {
  try {
    const data = JSON.parse(e.postData.contents);
    
    if (data.id && data.name) {
      return jsonResponse(addMerchant(data));
    }
    
    return jsonResponse({ error: '未知的操作' }, 400);
  } catch (error) {
    Logger.log('doPost 錯誤: ' + error);
    return jsonResponse({ error: error.toString() }, 500);
  }
}

/**
 * 新增商家
 */
function addMerchant(data) {
  try {
    const ss = SpreadsheetApp.openById(CONFIG.HQ_SHEET_ID);
    const sheet = ss.getSheetByName(CONFIG.SHEETS.MERCHANTS);
    
    if (!sheet) {
      return { success: false, message: '找不到商家清單分頁' };
    }
    
    sheet.appendRow([
      data.id,
      data.name,
      data.phone,
      data.serviceTime,
      data.maxStores,
      data.storeCount || 0,
      data.status || '啟用',
      data.note || ''
    ]);
    
    const logsSheet = ss.getSheetByName(CONFIG.SHEETS.LOGS);
    if (logsSheet) {
      logsSheet.appendRow([
        new Date(),
        '商家管理',
        '新增商家：' + data.name,
        '戰情室',
        '商家ID: ' + data.id
      ]);
    }
    
    return { success: true, message: '新增成功', id: data.id };
  } catch (error) {
    Logger.log('addMerchant 錯誤: ' + error);
    return { success: false, message: error.toString() };
  }
}
```

---

## ⚠️ 重要提醒

### 新增商家失敗的最常見原因：
**沒有「重新部署」Apps Script！**

即使你加了代碼，也要：
1. 部署 → 管理部署作業
2. 編輯 → **新版本**
3. 部署

不然前端還是連到舊版的 Web App！

---

## ✅ 修復後檢查清單

- [ ] 密碼保護正常運作（密碼：zxc88888）
- [ ] 輸入密碼後會記住（不用每次輸入）
- [ ] Web App URL 會自動載入
- [ ] 店家監控顯示詳細資訊（不是只說開發中）
- [ ] 新增商家成功
- [ ] 商家列表正常顯示

---

**我現在開始修復前端，5分鐘後推送！**
